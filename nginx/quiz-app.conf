server {
 listen 80;
 listen [::]:80;

 charset utf-8;
 access_log off;

 root /var/www/html;
 server_name bodymindstate.space www.bodymindstate.space;

 location / {
     proxy_pass http://quiz-app:8080;
     proxy_set_header Host $host:$server_port;
 }
}